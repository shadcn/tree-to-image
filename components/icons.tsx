import { Banana, File, Folder, Loader2 } from "lucide-react"
import type { Icon as LucideIcon } from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  folder: Folder,
  file: File,
  spinner: Loader2,
  brand: Banana,
}
